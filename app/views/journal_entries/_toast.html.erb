<% if flash.present? %>
  <div class="<%= class_names("bl_toast", { hp_notice: flash[:notice], hp_alert: flash[:alert] }) %>"
       data-controller="toast"
       data-toast-target="toast">
    <div class="bl_toast_message">
      <%= notice || alert %>
    </div>
    <!-- /.bl_toast_message -->
    <div class="el_closeBtn" data-action="click->toast#close">
      ✕
    </div>
  </div>
  <!-- /.bl_toast -->
<% end %>